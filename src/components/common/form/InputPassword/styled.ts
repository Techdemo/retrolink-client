import styled from 'styled-components';

import LockClosedIcon from 'vectors/lock-closed.svg';
import LockOpenIcon from 'vectors/lock-open.svg';

export const LockClosedIconStyled = styled(LockClosedIcon)`
  width: 20px;
`;

export const LockOpenIconStyled = styled(LockOpenIcon)`
  width: 20px;
`;
